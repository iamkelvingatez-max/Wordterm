# WordTerm (Windows)

This folder is for Windows-only notes/scripts. The main Electron app lives in the repo root.

## Dev quickstart
1) Install Node.js LTS
2) From repo root:
   - npm install
   - npm run
   - then run the appropriate script shown by npm run

## Windows notes
- If you need Windows-specific shell defaults, document them here.
